package net.victorbetoni.synergy.utils;

public class YMLUtils {
    /*public static JSONObject parseJSONObject(String dir) {
        try(BufferedReader reader = new BufferedReader(new InputStreamReader(JSONUtils.class.getResourceAsStream("/" + dir)))){
            Object obj = new JSONParser().parse(reader);
            return (JSONObject) obj;
        } catch (IOException | ParseException ex) {
            ex.printStackTrace();
        }
        return null;
    }*/
}
